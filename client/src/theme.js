import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2e7d32",
    },
    secondary: {
      main: "#BB6B00",
    },
    background: {
      default: "#210f04",
      paper: "#fcfffc",
    },
  },
});

export default theme;
